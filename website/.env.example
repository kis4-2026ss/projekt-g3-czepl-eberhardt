# URL of your running Directus instance
DIRECTUS_URL=http://localhost:8055

# Static API token from Directus → Settings → Access Tokens
# Alternatively: enable public Read access in Directus → Settings → Roles → Public
DIRECTUS_TOKEN=

# URL of the MCP server (used for preview/confirm flow)
MCP_SERVER_URL=http://localhost:3001
